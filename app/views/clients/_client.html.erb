

<i class="fa fa-user"></i> <strong><%= @client.name %></strong><br>

<% if !@client.description.empty? %>
  <i><%= @client.description %></i></br>
<% end %>
<%= render @client.address %>
<%= icon_to "glyphicon glyphicon-edit", edit_client_path(@client), "Modifier", class: 'pull-right' %>
