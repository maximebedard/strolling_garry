<% if address %>
  <address>
    <%= address.number %> <%= address.street %><br />
    <%= address.city %>, <%= address.province %>, <%= address.country %><br />
    <%= address.postal_code %>
  </address>
<% else %>
  N/A
<% end %>
