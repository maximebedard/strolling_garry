
<% flash.select {|k,_| %w(info success).include? k } .each do |type, message| %>
  <div class="alert <%= "alert-#{type}" %>">
    <button class="close" data-dismiss="alert">×</button>
    <%= message %>
  </div>
<% end %>
